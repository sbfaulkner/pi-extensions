# Agents

## Git Workflow

This repo uses standard GitHub PRs — **do not use Graphite (`gt`)**.
Use `git` and `gh` CLI for branching, pushing, and creating PRs.

**Never commit directly to `main`.** Always create a feature branch, push it, and open a PR.
