Agency extension

Simple experimental extension demonstrating a persistent widget above the editor (Pattern 5).

Commands:
- /agency           Toggle the widget on/off
- /agency add TEXT  Add a line to the widget

Install by adding this package to your Pi settings (repo already includes the extension in package.json pi.extensions).